
# get the correct docker-compose command for each
# ie. @docker-compose -f ./src/docker-compose.yml up -d -> breakdown why -f, why -d etc
#


run:
	Docker run ./srcs/requirements/ngnix/Dockerfile

all: up

up:

down:

stop:

start:

status:
